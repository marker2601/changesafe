# ChangeSafe

ChangeSafe is a metadata-aware pre-merge safety agent for analytics schema changes.

The complete setup, architecture, security, testing, and demonstration guide is added with the release package. Credential-free replay mode is the default.
